<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:background="@drawable/shape_rounded_corner_white"
    android:orientation="vertical"
    android:paddingBottom="10dp" >

    <RelativeLayout
        android:id="@+id/disconnect_header_layout"
        android:layout_width="match_parent"
        android:layout_height="@dimen/top_header_size"
        android:layout_marginTop="10dp" >

        <TextView
            android:id="@+id/disconnect_title_textview"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/tracker_in_progress"
            android:textColor="@color/violet_color"
            android:textSize="@dimen/heading_text_size"
            android:textStyle="bold" />
    </RelativeLayout>

    <View
        android:id="@+id/divider_view"
        android:layout_width="fill_parent"
        android:layout_height="1dp"
        android:layout_below="@+id/disconnect_header_layout"
        android:background="@color/purple_color" />

    <Button
        android:id="@+id/disconnect_cancel_all_button"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_margin="10dp"
        android:background="@drawable/shape_rounded_corner_red"
        android:padding="15dp"
        android:text="@string/cancelall"
        android:textColor="@color/white_color"
        android:textSize="@dimen/normal_text_size"
        android:textStyle="bold" />

</RelativeLayout>